{
  "dependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^2.1.1",
    "js-yaml": "^4.1.0",
    "ts-node": "^10.5.0",
    "vscode-json-languageservice": "^4.2.0"
  },
  "scripts": {
    "ajv": "ajv",
    "compile": "tsc -p ./src",
    "deps": "npx npm-check-updates -u && npm install",
    "lint": "pre-commit run -a",
    "test": "mocha"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/minimatch": "^3.0.5",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.18",
    "minimatch": "^5.0.0",
    "mocha": "^9.2.0",
    "typescript": "^4.5.5"
  },
  "directories": {
    "test": "./src"
  }
}
