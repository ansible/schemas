- hosts: localhost
  tags: # <-- allowed
    - foo
    - bar
  tasks:
    - ansible.builtin.debug:
        msg: "..."
      tags: # <-- allowed
        - foo
        - bar
    - ansible.builtin.debug:
        msg: "..."
      tags: # <-- allowed
        - foo
- hosts: localhost
  tags: foo # <-- allowed
